# 🏩 Техническая архитектура

## Обзор

```
════════════════════════════════════
║         🎤 Голосовой ассистент Windows 11       ║
════════════════════════════════════
              │
              │ config/settings.py
              │ (Конфигурация, параметры)
              │
     ┌───────┴───────┐
     │                    │
     ■ main.py            ■ VoiceAssistant
     │ (Основной диаспетчер)   │
     │                    │
     └───────┬───────┘
              │
     ┌──────────────────────────┐
     │♫ MODULES (modules/)                  │
     │♫                                      │
     │  ┘┬────────────────────────┐
     │     │
     │     ┏──────────────────────╮
     │     │  🎤 РЕЧОВОЙ ПОТОК           │
     │     │──────────────────────╯
     │     │  speech_recognition.py   │
     │     │  (Vosk ASR)              │
     │     │                         │
     │     │  text_to_speech.py       │
     │     │  (pyttsx3 TTS)           │
     │     │                         │
     │     └──────────────────────╯
     │
     │     ┏──────────────────────╮
     │     │  📄 КОМАНДНОЕ ОСТАНО            │
     │     │──────────────────────╯
     │     │  commands.py             │
     │     │  (CommandManager)        │
     │     │                         │
     │     │  nlp_processor.py        │
     │     │  (Intent recognition)    │
     │     │                         │
     │     └──────────────────────╯
     │
     │     ┏──────────────────────╮
     │     │  📠 ОБРАБОТКА ДАННЫХ           │
     │     │──────────────────────╯
     │     │  ocr_translator.py       │
     │     │  (Tesseract + Argos)     │
     │     │                         │
     │     │  system_monitor.py       │
     │     │  (CPU/GPU/Temp)          │
     │     │                         │
     │     └──────────────────────╯
     │
     └──────────────────────────┘
              │
              │
     ┌──────────────────────────┐
     │  📾 ВНЕШНИЕ СЕРВИСЫ              │
     │──────────────────────────│
     │  Porcupine (Wake-word)                 │
     │  Vosk (ASR)                           │
     │  pyttsx3 (TTS)                        │
     │  Tesseract (OCR)                      │
     │  Argos Translate (NMT)                │
     │  psutil/GPUtil (Monitoring)           │
     │  OpenAI ChatGPT (Optional)            │
     └──────────────────────────┘
```

---

## Поток данных

```python
МИКРОФОН
    │
    ↓ Пыловые данные (частота 48 кГц)
    │
    ↓ SpeechRecognizer (Vosk)
    │   - Непрерывное наслушивание в нити
    │   - Отрисовка рач текстом
    │   - Прием коллбэка on_result
    │
    ↓ NLPProcessor
    │   - Основа обыбработки текста
    │   - Охора намерение
    │   - Выход: intent, confidence
    │
    ↓ CommandManager
    │   - Поиск покохжей команды
    │   - Регистрация исполнение
    │
    ↓ Действие
    │   ┌─────────────────────────────┐
    │   │ ┗─ Открыть апликацию         │
    │   │ ┗─ Очитать экран (OCR)      │
    │   │ ┗─ Перевести текст         │
    │   │ ┗─ Показать статистику    │
    │   ┗─────────────────────────────┘
    │
    ↓ TextToSpeech (pyttsx3)
    │   - Преобразование текст в речь
    │   - Отда в докна
    │
    ↓
    ДИНАМИКИ
```

---

## Приоритеты проектирования

### Надежность
- Крашения вОзможные через exception handling
- Отдельные нити для основного процесса
- Грациюсное Очищение носурсов

### Оптимальность
- Основные айнто хранятся локально
- С винтыр данныя стрингуются
- Ясинхронные операции работают в нити

### Масштабируемость
- Модульная архитектура
- Команды регистрируются динамически
- Нет жестких связей между модулями

---

## Ом нити и синхронизация

```
Main Thread
  │
  │ while is_running:
  │   - Обновлят статистику
  │   - Пытаются выполненные команды
  │
  └── Speech Recognition Thread
      - Непрерывно слушает
      - Вызывает on_result
      - Не блокируют основнох цикл
```

---

## Видоисработки и расвитие

### Приоритет 1 (Core)
- ✓ Распознавание речи
- ✓ Синтез речи
- ✓ Мониторинг системы
- ✓ Командным менеджер

### Приоритет 2 (Enhancement)
- ✗ OCR и перевод
- ✗ NLP анализ
- ✗ Гуи интерфейс
- ✗ Логирование

### Приоритет 3 (Advanced)
- ChatGPT интеграция
- Динамическое обучение
- Графический интерфейс
- Локальная ВА

---

## Апи и Абстракция

### VoiceAssistant (main.py)
```python
class VoiceAssistant:
    def __init__(self) -> None
    def start(self) -> None
    def stop(self) -> None
    def process_command(self, user_input: str) -> None
    def _on_speech_recognized(self, text: str) -> None
```

### CommandManager (commands.py)
```python
class CommandManager:
    def register_command(self, command: Command) -> None
    def execute_command(self, command_name: str) -> bool
    def find_similar_command(self, user_input: str) -> Optional[Command]
    def get_all_commands(self) -> List[Command]
```

### SystemMonitor (system_monitor.py)
```python
class SystemMonitor:
    def get_cpu_stats(self) -> Dict[str, float]
    def get_gpu_stats(self) -> Dict[str, Optional[float]]
    def get_all_stats(self) -> SystemStats
    def format_stats(self, stats: SystemStats) -> str
```

---

## ОШИБКИ И НЕУДАЧЛО

### ОШИБКИ ПРИ ПОДУХОВАНИИ

```python
Attempt to load model from incorrect path
→ Точка решения:
   - Проверить settings.py
   - Очистить кэш
   - Перескачать модель
```

### ОШИБКИ Tesseract

```python
Tesseract not found
→ Точка решения:
   - Установить Новые Tesseract
   - Управните путь в settings.py
```

---

## Метрики и Производительность

| Метрика | Мтоп | ГОЛ |
|-----------|---------|-----|
| Точность Vosk | ~85% | >90% |
| Латенция | <500ms | <200ms |
| Употребление ОЗУ | <100MB | <50MB |
| Температура CPU | <50°C | <40°C |

---

**Основное начало: Оптимизация на вторые ролевые лица**
