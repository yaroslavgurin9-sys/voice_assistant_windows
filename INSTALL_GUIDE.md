# 🚀 Наполнительное руководство по установке в PyCharm

## Шаг 1: Подготовка зависимостей

### Установка Tesseract OCR

1. Открыть: https://github.com/UB-Mannheim/tesseract/wiki
2. Скачать ПО для Windows
3. Установить в `C:\Program Files\Tesseract-OCR\`
4. Проверить в cmd:
   ```bash
   tesseract --version
   ```

### Скачивание моделей Vosk

1. Пеойти на https://alphacephei.com/vosk/models
2. Скачать `vosk-model-ru-0.42.zip`
3. Создать фолдер:
   ```
   voice_assistant_windows/
   ├── models/
   └─── vosk-model-ru-0.42/
   ```
4. Распаковать там архив

---

## Шаг 2: PyCharm Конфигурация

### Открыть проект

```bash
# Открыть в PyCharm
pycharm voice_assistant_windows
```

### Настроить интерпретатор Python

1. **File** > **Settings** (или **PyCharm** > **Preferences** на Mac)
2. **Project** > **Python Interpreter**
3. Нажать гербик (⚙️) в правом углу
4. Выбрать **Add Interpreter** > **Add Local Interpreter**
5. Выбрать существующее окружение:
   ```
   .\venv\Scripts\python.exe
   ```

### Установка зависимостей

1. Открыть **Terminal** в PyCharm
2. Выполнить:
   ```bash
   pip install -r requirements.txt
   ```
3. При сообщениях об ошибках PyAudio:
   ```bash
   pip install pipwin
   pipwin install pyaudio
   ```

---

## Шаг 3: Конфигурация стандартной запусков Программы

1. **Run** > **Edit Configurations**
2. Нажать **+** в левом столбце
3. Выбрать **Python**
4. Настроить:
   - **Name**: "Voice Assistant"
   - **Script path**: выбрать `main.py`
   - **Python interpreter**: использовать виртоальное окружение
   - **Working directory**: корень проекта
5. Нажать **OK**

---

## Шаг 4: Первый запуск

### Проверить модели

1. Убедитесь, что модель Vosk на месте:
   ```
   models/vosk-model-ru-0.42/
   ```

2. Отедите `.env` файл и добавьте свои API ключи (опционально)

### Запустить проект

1. Нажать **Run** > **Run...** (оли `Shift + F10`)
2. Выбрать у вниз если требуется
3. Открыть консоль: **View** > **Tool Windows** > **Run**
4. Микрофон должен слушать

---

## Проверка доступных голосов

1. Открыть поновый **Python Console** в PyCharm
2. Выполнить:
   ```python
   import pyttsx3
   engine = pyttsx3.init()
   voices = engine.getProperty('voices')
   for voice in voices:
       print(f"{voice.name}: {voice.languages}")
   ```

---

## Отладка и гашение на нежеланные выводы

- **Ctrl+F5** - Перезапустить
- **Ctrl+C** в консоли - Остановить
- **View** > **Tool Windows** > **Debug** - Открыть дебаггер

---

## Очистка проекта

```bash
# Очистить кэш Python
pytest --cache-clear

# Очистить кэш PyCharm
# На Windows: C:\Users\%USERNAME%\AppData\Local\JetBrains\PyCharm\system\caches
```

---

## Усовная подсказка

- Отредактируйте `config/settings.py` для персонализации
- Запусках с `--debug` точность лючше
- Все логи сохраняются в `logs/`
